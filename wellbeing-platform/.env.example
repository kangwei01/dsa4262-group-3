Copy this file to .env and fill in values if connecting to a live backend.
For local demo mode, leave VITE_APP_ID as-is — the app will run without authentication.
VITE_APP_ID=your_app_id
VITE_APP_BASE_URL=your_backend_url
VITE_FUNCTIONS_VERSION=prod
