# Screenshots

Add the following screenshots here for the README:

- `checkin.png` — student check-in survey view (capture from localhost:5173)
- `dashboard.png` — teacher dashboard showing risk scores and student list (capture from localhost:5173/teacher after teacher login)

Once captured, place the PNG files in this folder and the README will render them automatically.
